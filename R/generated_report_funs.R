# generated by dbc::generate_script_from_expressions.
# do no modify by hand!

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_all_are_TRUE <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "all(x %in% TRUE)"
  fail_messages <- 
    "Not all elements of `${x_nm}` were TRUE"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_atom_is_in_set <- function(
  x,
  x_nm = NULL,
  call = NULL,
  set
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1", "x %in% set")
  fail_messages <- 
    c(NA, "object `${x_nm}` with value `${deparse1(x)}` was not in set of expected values (first ten): `${deparse1(utils::head(set, 10L))}`")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_data_table_has_no_duplicates <- function(
  x,
  x_nm = NULL,
  call = NULL,
  by = names(x)
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "!(dup <- duplicated(x, by = by))"
  fail_messages <- 
    "In total ${n_fail} rows of data.table ${deparse1(x_nm)} were duplicates (by ${deparse1(by)}); first five row numbers that were duplicated: ${deparse1(utils::head(which(dup), 5))}"
  pass_messages <- 
    "data.table ${deparse1(x_nm)} had no duplicates (by ${deparse1(by)})"
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_dir_exists <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "length(bad_dirs <- x[!dir.exists(x)]) == 0L"
  fail_messages <- 
    "the following directory/directories in object `${x_nm}` do not exist: ${deparse(bad_dirs)}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_file_exists <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "length(bad_files <- x[!file.exists(x)]) == 0L"
  fail_messages <- 
    "the following file(s) in object `${x_nm}` do not exist: ${bad_files}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_class <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_class
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, required_class)"
  fail_messages <- 
    "expected object `${x_nm}` to have class ${required_class}, but it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_length <- function(
  x,
  x_nm = NULL,
  call = NULL,
  expected_length
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "length(x) == expected_length"
  fail_messages <- 
    "object `${x_nm}` had length ${length(x)} instead of ${expected_length}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("!is.null(names(x))", "length(miss_nms <- setdiff(required_names, names(x))) == 0L")
  fail_messages <- 
    c("object `${x_nm}` did not have any names", "object `${x_nm}` did not have the following expected names: ${deparse(miss_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_no_duplicates <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "!duplicated(x)"
  fail_messages <- 
    "In total ${n_fail} elements of object/param `${x_nm}` were duplicates; first five values that were duplicated: ${deparse(utils::head(unique(x[duplicated(x)]), 5L))}"
  pass_messages <- 
    "object `${x_nm}` had no duplicates"
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_one_of_classes <- function(
  x,
  x_nm = NULL,
  call = NULL,
  classes
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, classes)"
  fail_messages <- 
    "expected object `${x_nm}` to have one of classes ${deparse1(classes)}, but it had class(es) ${deparse1(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_has_only_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("!is.null(names(x))", "length(miss_nms <- setdiff(required_names, names(x))) == 0L", "length(extra_nms <- setdiff(names(x), required_names)) == 0L")
  fail_messages <- 
    c("object `${x_nm}` did not have any names", "object `${x_nm}` did not have the following expected names: ${deparse(miss_nms)}", "object `${x_nm}` had the following unexpected names: ${deparse(extra_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_inherits <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_class
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, required_class)"
  fail_messages <- 
    "expected object `${x_nm}` to have class ${required_class}, but it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_assertion_type <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1", "is.character(x)", "!is.na(x)", "x %in% dbc::assertion_types()")
  fail_messages <- 
    c("`${x_nm}` was not atomic", "`${x_nm}` was not a string", "`${x_nm}` was NA", "`${x_nm}` was not one of the strings given by dbc::assertion_types()")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "length(x) == 1L"
  fail_messages <- 
    "length of object `${x_nm}` was ${length(x)} instead of 1"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_between_exclusive <- function(
  x,
  x_nm = NULL,
  call = NULL,
  lo,
  hi
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "dbc::is_between_exclusive(x = x, lo = lo, hi = hi)"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were outside exclusive bounds ${lo}, ${hi}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_between_inclusive <- function(
  x,
  x_nm = NULL,
  call = NULL,
  lo,
  hi
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "dbc::is_between_inclusive(x = x, lo = lo, hi = hi)"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were outside inclusive bounds ${lo}, ${hi}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_call <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.call(x)"
  fail_messages <- 
    "object `${x_nm}` was not a call object; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.character(x)"
  fail_messages <- 
    "object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.character(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.character(x)", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.character(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.character(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.character(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_character_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.character(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class character; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data.frame <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "{warning(\"*_is_data.frame/table* functions will be removed in a future release; use *_is_data_frame/table* functions instead (fully snake_case)\"); is.data.frame(x)}"
  fail_messages <- 
    "object `${x_nm}` was not a data.frame; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data.frame_with_required_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.data.frame(x)", "{warning(\"*_is_data.frame/table* functions will be removed in a future release; use *_is_data_frame/table* functions instead (fully snake_case)\"); length(miss_nms <- setdiff(required_names, names(x))) == 0L}")
  fail_messages <- 
    c("object `${x_nm}` was not a data.frame; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` did not have the following expected columns: ${deparse(miss_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data.table <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "{warning(\"*_is_data.frame/table* functions will be removed in a future release; use *_is_data_frame/table* functions instead (fully snake_case)\"); inherits(x, \"data.table\")}"
  fail_messages <- 
    "object `${x_nm}` was not a data.table; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data.table_with_required_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"data.table\")", "{warning(\"*_is_data.frame/table* functions will be removed in a future release; use *_is_data_frame/table* functions instead (fully snake_case)\"); length(miss_nms <- setdiff(required_names, names(x))) == 0L}")
  fail_messages <- 
    c("object `${x_nm}` was not a data.table; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` did not have the following expected columns: ${deparse(miss_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data_frame <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.data.frame(x)"
  fail_messages <- 
    "object `${x_nm}` was not a data.frame; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data_frame_with_required_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.data.frame(x)", "length(miss_nms <- setdiff(required_names, names(x))) == 0L")
  fail_messages <- 
    c("object `${x_nm}` was not a data.frame; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` did not have the following expected columns: ${deparse(miss_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data_table <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, \"data.table\")"
  fail_messages <- 
    "object `${x_nm}` was not a data.table; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_data_table_with_required_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"data.table\")", "length(miss_nms <- setdiff(required_names, names(x))) == 0L")
  fail_messages <- 
    c("object `${x_nm}` was not a data.table; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` did not have the following expected columns: ${deparse(miss_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, \"Date\")"
  fail_messages <- 
    "object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "inherits(x, \"Date\")")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"Date\")", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "inherits(x, \"Date\")", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"Date\")", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"Date\")", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_Date_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"Date\")", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class Date; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.double(x)"
  fail_messages <- 
    "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] >= 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] >= 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] >= 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] > 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] > 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] > 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] <= 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] <= 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] <= 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] < 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] < 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] < 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] >= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] >= 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] >= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] > 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] > 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] > 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] <= 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.double(x)", "x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] < 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_double_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.double(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class numeric; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_environment <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.environment(x)"
  fail_messages <- 
    "object `${x_nm}` was not an environment object"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_expression <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.expression(x)"
  fail_messages <- 
    "object `${x_nm}` was not an `expression` object; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.factor(x)"
  fail_messages <- 
    "object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.factor(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.factor(x)", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.factor(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.factor(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.factor(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.factor(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class factor; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_factor_with_levels <- function(
  x,
  x_nm = NULL,
  call = NULL,
  expected_levels
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.factor(x)", "length(extra_levels <- setdiff(levels(x), expected_levels)) == 0L", "length(miss_levels <- setdiff(expected_levels, levels(x))) == 0L")
  fail_messages <- 
    c("object `${x_nm}` is not a factor; instead it had class(es) ${deparse(class(x))}", "factor object `${x_nm}` had these unexpected levels: ${deparse(extra_levels)}", "factor object `${x_nm}` did not have these expected levels: ${deparse(miss_levels)}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_function <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.function(x)"
  fail_messages <- 
    "object `${x_nm}` was not a function; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_function_with_required_argument_names <- function(
  x,
  x_nm = NULL,
  call = NULL,
  required_argument_names
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.function(x)", "all(required_argument_names %in% names(formals(x)))")
  fail_messages <- 
    c("object `${x_nm}` was not a function", "object `${x_nm}` did not have all required arguments ${deparse(required_argument_names)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_gt <- function(
  x,
  x_nm = NULL,
  call = NULL,
  lo
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] > lo"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were <= ${lo}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_gte <- function(
  x,
  x_nm = NULL,
  call = NULL,
  lo
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] >= lo"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were < ${lo}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_gtezero <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] >= 0"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were < 0"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_gtzero <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] > 0"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were <= 0"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.integer(x)"
  fail_messages <- 
    "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] >= 0", "is.integer(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.integer(x)", "is.matrix(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.integer(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] > 0", "is.integer(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.integer(x)", "is.matrix(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.integer(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)", "x[!is.na(x)] <= 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] <= 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] <= 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)", "x[!is.na(x)] < 0")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] < 0", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] < 0", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] >= 0", "is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.integer(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] > 0", "is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.integer(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)", "x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] <= 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.integer(x)", "x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] < 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_integer_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.integer(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class integer; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_language_object <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.language(x)"
  fail_messages <- 
    "object `${x_nm}` was not an R language object; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_list <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "inherits(x, \"list\")"
  fail_messages <- 
    "object `${x_nm}` was not of class list"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.logical(x)"
  fail_messages <- 
    "object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.logical(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.logical(x)", "is.matrix(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.logical(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.logical(x)", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE")
  fail_messages <- 
    c("object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.logical(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_logical_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.logical(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class logical; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_lt <- function(
  x,
  x_nm = NULL,
  call = NULL,
  hi
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] < hi"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were >= ${hi}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_lte <- function(
  x,
  x_nm = NULL,
  call = NULL,
  hi
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] <= hi"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were > ${hi}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_ltezero <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] <= 0"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were > 0"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_ltzero <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x[!is.na(x)] < 0"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were >= 0"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.matrix(x)"
  fail_messages <- 
    "object `${x_nm}` was not a matrix"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_name <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.name(x)"
  fail_messages <- 
    "object `${x_nm}` was not a name object; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_named <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "!is.null(names(x))"
  fail_messages <- 
    "object `${x_nm}` did not have any names"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_named_list <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"list\")", "!is.null(names(x))")
  fail_messages <- 
    c("object `${x_nm}` was not of class list", "object `${x_nm}` did not have any names")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_nonNA <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE"
  fail_messages <- 
    "object `${x_nm}` had ${n_fail} NA values - none are allowed"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_NULL <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.null(x)"
  fail_messages <- 
    "object `${x_nm}` was not NULL"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.numeric(x)"
  fail_messages <- 
    "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] >= 0", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.matrix(x)", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] > 0", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.matrix(x)", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] <= 0", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] <= 0", "is.matrix(x)", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] <= 0", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] < 0", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] < 0", "is.matrix(x)", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] < 0", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.matrix(x)", "is.numeric(x)")
  fail_messages <- 
    c("object `${x_nm}` was not a matrix", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] >= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] >= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were < 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] > 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_gtzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] > 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were <= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltezero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltezero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] <= 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltezero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] <= 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were > 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltzero_atom <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("length(x) == 1L", "x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("length of object `${x_nm}` was ${length(x)} instead of 1", "${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltzero_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] < 0", "is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_ltzero_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("x[!is.na(x)] < 0", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("${n_fail} elements of `${x_nm}` were >= 0", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_matrix <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.matrix(x)", "if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)")
  fail_messages <- 
    c("object `${x_nm}` was not a matrix", "object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_nonNA_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("if (is.vector(x) && !is.list(x)) !is.na(x) else TRUE", "is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` had ${n_fail} NA values - none are allowed", "object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_number_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.numeric(x)", "is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` was not a number; instead it had class(es) ${deparse(class(x))}", "object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_numeric <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.numeric(x)"
  fail_messages <- 
    "object `${x_nm}` was not numeric; instead it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_of_length <- function(
  x,
  x_nm = NULL,
  call = NULL,
  expected_length
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "length(x) == expected_length"
  fail_messages <- 
    "object `${x_nm}` had length ${length(x)} instead of ${expected_length}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_report_df <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.data.frame(x)", "length(miss_col_nms <- setdiff(names(get_report_df_template()), names(x))) == 0L")
  fail_messages <- 
    c("`${x_nm}` was not a data.frame", "Following columns were expected but not in `${x_nm}`: ${deparse(miss_col_nms)}")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_subset_of_data_frame <- function(
  x,
  x_nm = NULL,
  call = NULL,
  y,
  y_nm = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.data.frame(y)", "is.data.frame(x)", "names(x) %in% names(y)", "{nrow(merge(x = x, y = y[, names(x)], by = names(x), all.x = FALSE, all.y = FALSE)) == nrow(x)}")
  fail_messages <- 
    c("Internal error: `${y_nm}` to test against was not a data.frame", "`${x_nm}` to test was not a data.frame", "Not all column names of `${x_nm}` were found in `${y_nm}` to test against", "Not all rows of `${x_nm}` were found in `${y_nm}`.")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_subset_of_data_table <- function(
  x,
  x_nm = NULL,
  call = NULL,
  y,
  y_nm = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(y, \"data.table\")", "is.data.frame(x)", "names(x) %in% names(y)", "{nrow(merge(x = x, y = y[, names(x)], by = names(x), all.x = FALSE, all.y = FALSE)) == nrow(x)}")
  fail_messages <- 
    c("Internal error: `${y_nm}` to test against was not a data.table", "`${x_nm}` was not a data.frame", "Not all column names of `${x_nm}` were found in `${y_nm}` to test against", "Not all rows of `${x_nm}` were found in `${y_nm}`.")
  pass_messages <- 
    c(NA_character_, NA_character_, NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_TRUE <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "x %in% TRUE"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` were FALSE or NA"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_unevaluated_expression <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "is.language(x) && !inherits(x, \"formula\")"
  fail_messages <- 
    "object `${x_nm}` was not an expression object that can be evaluated by `eval`; it had class(es) ${deparse(class(x))}"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_uniquely_named <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "(n_unique_names <- length(unique(names(x)))) == length(x)"
  fail_messages <- 
    "`${x_nm}` has ${n_unique_names} unique names but is of length ${length(x)}; expected the same numbers"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_uniquely_named_list <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("inherits(x, \"list\")", "(n_unique_names <- length(unique(names(x)))) == length(x)")
  fail_messages <- 
    c("object `${x_nm}` was not of class list", "not every element of object `${x_nm}` has a different name")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_is_vector <- function(
  x,
  x_nm = NULL,
  call = NULL
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    c("is.null(dim(x))", "!is.list(x)")
  fail_messages <- 
    c("object `${x_nm}` had dimensions but was expected to have none", "object `${x_nm}` was a list")
  pass_messages <- 
    c(NA_character_, NA_character_)
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_match_regex <- function(
  x,
  x_nm = NULL,
  call = NULL,
  grepl.arg.list = list()
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "{grepl.arg.list[[\"x\"]] <- x; t <- do.call(grepl, grepl.arg.list); t}"
  fail_messages <- 
    "${n_fail} elements of `${x_nm}` did not match regex ${deparse(grepl.arg.list[[\"pattern\"]])}"
  pass_messages <- 
    "All elements of `${x_nm}` matched regex ${deparse(grepl.arg.list[[\"pattern\"]])}"
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

# generated by dbc::generate_script_from_expressions.
# do no modify by hand!
#' @rdname assertions
#' @export
report_vector_elems_are_in_set <- function(
  x,
  x_nm = NULL,
  call = NULL,
  set
) {
  dbc::handle_args_inplace()
  
  expressions <- 
    "in_set <- x %in% set"
  fail_messages <- 
    "some values of object `${x_nm}` were not in set of expected values. First ten bad values: `${deparse1(utils::head(unique(x[!in_set]), 10L))}`. First ten elements in set of expected values: `${deparse1(utils::head(set, 10L))}`"
  pass_messages <- 
    NA_character_
  report_env <- environment()
  report_df <- dbc::expressions_to_report(
    expressions = expressions,
    fail_messages = fail_messages,
    pass_messages = pass_messages,
    env = report_env, 
    call = call
  )
  return(report_df)
}

